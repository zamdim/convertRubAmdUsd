export enum LocalStorageKeysEnum {
  TOKEN = 'token',
  ID = 'id',
}

export enum BoolEnums {
  TRUE = 'true',
  FALSE = 'false',
}

export enum PaginationEnum {
  PAGE = 'page',
  PER_PAGE = 'limit',
}

export enum SortDirectionEnum {
  ASCENDING = 'true',
  DESCENDING = 'false',
  SORT = 'sort',
  SORT_DESC = 'sort_desc',
}

export enum FilterDataEnum {
  CDATES = 'cdates',
}

export enum ContextItemType {
  BLOCKED,
}

export const enum RequestStatusEnum {
  IDLE = 'idle',
  PENDING = 'pending',
  FULFILLED = 'fulfilled',
  REJECTED = 'rejected',
}
